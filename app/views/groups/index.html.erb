<body class="page5">

  <div class="group-index-page">
    Your Groups!
  </div>

  <div class="group-content">
    <% @relevant_groups.each do |group| %>
      <div class="small-12 medium-4 columns groupies">
        <%= link_to "#{group.name}", group_path(group) %></br>
        <%= group.city %>
      </div>
    <% end %>
  </div>

</body>
